def divide(a: float, b: float) -> float:
    """计算两个数相除的结果。

    这是供 SWE Agent 修复的示例函数。当前实现故意违反函数约定，
    配套测试会把这个缺陷暴露给 Agent。

    Args:
        a: 被除数。
        b: 除数。

    Returns:
        按函数约定应返回 ``a / b`` 的结果。
    """
    return a * b

# KnowFlowAgent 协作原则

## 项目目标

本项目是一个教学型、可运行的最小 SWE Agent。能力可以很窄，使用体验可以不完善，但最终产物必须真实可用：模型需要根据当前任务和真实工具结果自主选择下一步动作，能够读取代码、修改代码、运行测试，并依据反馈继续行动或结束。

第一阶段不追求通用性。优先级从高到低依次为：

1. 逻辑清晰，学习者能够解释整体运行过程。
2. 每一步都能运行、测试和观察结果。
3. 安全边界明确，模型不能绕过权限直接产生副作用。
4. 代码容易继续迭代。
5. 功能数量和使用便利性。

## 学习者背景

学习者具备软件工程学习经历，但 Python 语法基础较弱，部分语法已经忘记。不要求学习者脱离工具默写语法，但必须帮助其真正理解：

- 程序整体怎样运行；
- 每个模块为什么存在；
- 数据如何在模块间流动；
- 每个关键函数的输入、输出和副作用；
- Agent 如何观察、决策、执行并获得结果；
- 权限控制和安全边界为什么必要；
- 如何通过测试判断代码是否正确；
- 出错时如何根据证据定位问题，而不是让 AI 盲目修改。

遇到新的 Python 语法时，应结合当前代码简短解释其作用，不把语法教学脱离项目单独展开。

## 教学式实施原则

不得一次性生成完整项目后只做总结。实现必须按可运行的垂直里程碑推进，每个里程碑都要形成明确反馈：

1. 开始前说明这一小步要解决的问题，以及暂时不解决什么。
2. 只增加当前里程碑必需的代码，避免提前抽象和扩展。
3. 运行相关命令或测试，展示真实结果。
4. 解释新增或修改的模块、关键函数、数据流和副作用。
5. 说明失败时应读取哪些证据，以及为什么做当前修改。
6. 完成一个很小的理解检查，再进入下一里程碑。

所有“完成”“修复”“测试通过”的结论都必须有当前运行结果支持。

## 第一阶段里程碑

1. 创建一个包含真实错误和失败测试的 Python 示例项目。
2. 实现并单独验证四个工具：列出文件、读取文件、写入 Python 文件、运行 `pytest -q`。
3. 加入静态权限边界，并验证正常路径允许、越界路径和非白名单操作被拒绝。
4. 使用可预测的假模型实现并测试“决策 -> 执行 -> 观察 -> 再次决策”的 Agent 循环。
5. 接入 DeepSeek，让真实模型通过同一接口选择工具。
6. 完成端到端演示：Agent 修复示例项目，使测试从失败变为通过，并输出完整运行记录。

中间里程碑可以暂时不是完整 SWE Agent，但最终 MVP 必须包含真实模型驱动的反馈循环，不能用固定步骤脚本冒充 Agent。

## MVP 范围边界

第一阶段只提供以下能力：

- `list_files`：列出工作区内文件；
- `read_file`：读取工作区内文件；
- `write_file`：仅修改工作区内允许的 `.py` 文件；
- `run_tests`：只执行固定命令 `pytest -q`。

第一阶段不实现任意 Shell、网络工具、插件系统、多 Agent、并发执行、通用任务规划、复杂配置、自动提交或面向生产环境的沙箱。

DeepSeek 必须位于独立模型适配层。Agent 核心逻辑依赖统一模型接口；自动测试使用可预测的假模型，避免网络、费用和模型随机性影响测试。

## 安全和诊断原则

- 模型只能提出结构化工具调用，不能直接读写文件或执行命令。
- 所有工具调用必须先经过权限检查，再进入产生副作用的工具层。
- 文件路径必须解析后确认仍位于指定工作区内。
- 拒绝操作也要返回结构化观察，使 Agent 能看到失败原因。
- Agent 循环必须限制最大步数，避免无限执行。
- 遇到错误时先保存并阅读异常、工具输出和测试失败信息，再提出最小修改。
- 不得为了让测试变绿而删除、跳过或弱化测试，除非测试本身确有错误且已经说明证据。

## 代码清晰度原则

- 每个模块只承担一种主要职责。
- 对跨模块传递的数据使用名称明确、结构简单的类型。
- 函数尽量短，输入和返回值清楚；产生文件或进程副作用的函数必须容易识别。
- 优先使用 Python 标准库和少量必要依赖。
- 注释解释“为什么”，不重复代码已经表达的“做什么”。
- 不为假设中的未来需求提前设计复杂抽象。
